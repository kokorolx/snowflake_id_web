<%= form_with(model: master_id) do |form| %>
  <% if master_id.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(master_id.errors.count, "error") %> prohibited this master_id from being saved:</h2>

      <ul>
        <% master_id.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :snowflake_id, style: "display: block" %>
    <%= form.text_field :snowflake_id %>
  </div>

  <div>
    <%= form.label :snowflake_data, style: "display: block" %>
    <%= form.text_field :snowflake_data %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
